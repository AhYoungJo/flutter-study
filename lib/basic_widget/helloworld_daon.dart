import 'package:flutter/material.dart';

class helloworld_daon extends StatelessWidget {
  const helloworld_daon({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("안녕 세계"),
    );
  }
}
